#!/usr/bin/env python3
"""Audit an implementation diff against an independently approved contract."""


def audit_change(repo_root, contract_path, base_ref, head_ref):
    raise NotImplementedError('implement after oracle approval')
